export const beforeCallbacks = [];

export const beforeNavigate = (/* callback */) => {
  // Silence
};

export const goto = () => {
  // Silence
};

export const invalidateAll = () => {
  // Silence
};
